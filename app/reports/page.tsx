"use client";
import { useEffect, useState } from "react";
import { AppShell } from "@/components/app-shell";
import { PageHeader } from "@/components/page-header";
import { formatINR } from "@/lib/utils";
export default function ReportsPage(){const [d,setD]=useState<any>();useEffect(()=>{fetch('/api/reports').then(r=>r.json()).then(r=>setD(r.data))},[]);if(!d)return <AppShell><div className="p-8">Loading reports…</div></AppShell>;return <AppShell><div className="mx-auto max-w-[1200px] p-5 md:p-8"><PageHeader title="Reports" description="Receivables, reminder delivery, and payment-method performance."/><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><div className="card p-5"><p className="label">Customers</p><p className="mt-3 text-3xl font-bold">{d.customers}</p></div><div className="card p-5"><p className="label">Invoices</p><p className="mt-3 text-3xl font-bold">{d.invoices}</p></div><div className="card p-5"><p className="label">Outstanding</p><p className="mt-3 text-3xl font-bold">{formatINR(d.outstanding)}</p></div><div className="card p-5"><p className="label">Overdue</p><p className="mt-3 text-3xl font-bold text-rose-600">{formatINR(d.overdue)}</p></div></div><div className="mt-5 grid gap-5 lg:grid-cols-2"><div className="card p-6"><h2 className="font-bold">Collection summary</h2><div className="mt-5 space-y-3"><div className="flex justify-between"><span className="text-slate-500">Billed</span><b>{formatINR(d.billed)}</b></div><div className="flex justify-between"><span className="text-slate-500">Collected</span><b className="text-emerald-600">{formatINR(d.collected)}</b></div><div className="flex justify-between"><span className="text-slate-500">Outstanding</span><b>{formatINR(d.outstanding)}</b></div></div></div><div className="card p-6"><h2 className="font-bold">Reminder performance</h2><div className="mt-5 space-y-3"><div className="flex justify-between"><span className="text-slate-500">Delivered/sent</span><b>{d.remindersSent}</b></div><div className="flex justify-between"><span className="text-slate-500">Failed</span><b className="text-rose-600">{d.remindersFailed}</b></div>{Object.entries(d.byChannel).map(([k,v])=><div className="flex justify-between border-t pt-3" key={k}><span className="text-slate-500">{k}</span><b>{String(v)}</b></div>)}</div></div></div><div className="card mt-5 p-6"><h2 className="font-bold">Payment methods</h2><div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-5">{Object.entries(d.byPaymentMethod).map(([k,v])=><div key={k} className="rounded-xl bg-slate-50 p-4"><p className="text-xs font-semibold text-slate-400">{k}</p><p className="mt-2 font-bold">{formatINR(Number(v))}</p></div>)}</div></div></div></AppShell>}
