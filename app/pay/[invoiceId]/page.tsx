"use client";
import { useEffect, useState } from "react";
import { CheckCircle2, Link2, ShieldCheck, WalletCards } from "lucide-react";
import { formatINR } from "@/lib/utils";
export default function PublicPaymentPage({ params }: { params: Promise<{ invoiceId: string }> }) { const [id,setId]=useState(''); const [data,setData]=useState<any>(); const [busy,setBusy]=useState(false); useEffect(()=>{params.then(p=>setId(p.invoiceId))},[params]); useEffect(()=>{if(!id)return;fetch(`/api/public/invoices/${id}`).then(r=>r.json()).then(r=>setData(r.data))},[id]); async function pay(){setBusy(true);const r=await fetch(`/api/public/invoices/${id}/payment-link`,{method:'POST'});const d=await r.json();setBusy(false);if(!r.ok)return alert(d.error||'Unable to create payment link');window.location.href=d.data.shortUrl;} if(!data)return <main className="grid min-h-screen place-items-center bg-[#f7f8fc] p-6"><div className="card p-8">Loading payment request…</div></main>;return <main className="grid min-h-screen place-items-center bg-[#f7f8fc] p-6"><div className="w-full max-w-lg"><div className="mb-5 flex items-center gap-3"><div className="grid size-11 place-items-center rounded-xl bg-brand text-white"><WalletCards/></div><div><p className="font-bold">PayTrack AI</p><p className="text-xs text-slate-500">Secure payment request</p></div></div><div className="card p-7"><div className="flex items-start justify-between gap-4"><div><p className="text-sm text-slate-500">Invoice</p><h1 className="mt-1 text-2xl font-bold">{data.invoiceNumber}</h1><p className="mt-2 text-sm text-slate-500">{data.customerName}</p></div><span className={`rounded-full px-3 py-1 text-xs font-bold ${data.status==='PAID'?'bg-emerald-50 text-emerald-700':'bg-amber-50 text-amber-700'}`}>{data.status}</span></div><div className="my-7 rounded-2xl bg-slate-50 p-5"><p className="text-sm text-slate-500">Amount due</p><p className="mt-2 text-4xl font-bold">{formatINR(data.dueAmount)}</p><p className="mt-2 text-xs text-slate-400">Due date: {new Date(data.dueDate).toLocaleDateString('en-IN')}</p></div>{data.status!=='PAID'?<button disabled={busy} onClick={pay} className="flex w-full items-center justify-center gap-2 rounded-xl bg-brand px-5 py-3.5 text-sm font-bold text-white hover:bg-indigo-600 disabled:opacity-60">{busy?'Creating secure payment…':<>Pay securely <Link2 size={16}/></>}</button>:<div className="flex items-center justify-center gap-2 rounded-xl bg-emerald-50 p-4 text-sm font-semibold text-emerald-700"><CheckCircle2 size={18}/>Payment completed</div>}<div className="mt-5 flex items-center justify-center gap-2 text-xs text-slate-400"><ShieldCheck size={14}/>Encrypted checkout • INR payments</div></div></div></main> }
