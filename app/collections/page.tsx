"use client";
import { useEffect, useState } from "react";
import { MessageCircle, Mail, Phone, RefreshCw } from "lucide-react";
import { AppShell } from "@/components/app-shell";
import { PageHeader } from "@/components/page-header";
import { Button } from "@/components/ui/button";
export default function CollectionsPage(){const [logs,setLogs]=useState<any[]>([]); async function load(){const r=await fetch('/api/reminders');const d=await r.json();setLogs(d.data||[])} useEffect(()=>{load()},[]); return <AppShell><div className="mx-auto max-w-[1500px] p-5 md:p-8"><PageHeader title="Collections" description="Every reminder, delivery attempt, and failure in one place." action={<Button variant="secondary" onClick={load}><RefreshCw size={16}/>Refresh</Button>}/><div className="card overflow-hidden"><div className="overflow-x-auto"><table className="w-full min-w-[850px] text-left text-sm"><thead className="border-b bg-slate-50 text-xs uppercase tracking-wide text-slate-400"><tr><th className="px-6 py-3">Channel</th><th className="px-6 py-3">Invoice</th><th className="px-6 py-3">Customer</th><th className="px-6 py-3">Status</th><th className="px-6 py-3">Sent</th><th className="px-6 py-3">Error</th></tr></thead><tbody>{logs.map(l=>{const Icon=l.channel==='WHATSAPP'?MessageCircle:l.channel==='EMAIL'?Mail:Phone; return <tr key={l.id} className="border-b last:border-0"><td className="px-6 py-4"><span className="inline-flex items-center gap-2 rounded-lg bg-slate-50 px-2.5 py-1 text-xs font-semibold"><Icon size={14}/>{l.channel}</span></td><td className="px-6 py-4 font-semibold text-brand">{l.invoice.invoiceNumber}</td><td className="px-6 py-4">{l.invoice.customer.customerName}</td><td className="px-6 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-bold ${l.status==='FAILED'?'bg-rose-50 text-rose-600':l.status==='DELIVERED'?'bg-emerald-50 text-emerald-700':'bg-blue-50 text-blue-600'}`}>{l.status}</span></td><td className="px-6 py-4 text-slate-500">{l.sentAt?new Date(l.sentAt).toLocaleString('en-IN'):'—'}</td><td className="max-w-[300px] truncate px-6 py-4 text-xs text-rose-500">{l.error||'—'}</td></tr>})}</tbody></table>{!logs.length&&<div className="p-10 text-center text-sm text-slate-500">No collection activity yet.</div>}</div></div></div></AppShell>}
